language: node_js
node_js:
  - "8"

install:
  - npm install

script:
  - npm run test:coverage && npm run test:integration:setup && npm run test:integration

after_success:
  - npm install coveralls && nyc report --reporter=text-lcov | coveralls

after_script:
  - npm run test:integration:cleanup
